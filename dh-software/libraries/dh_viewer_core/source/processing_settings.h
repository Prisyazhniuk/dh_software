#pragma once

namespace dh
{
    struct processing_settings
    {
        float lambda_mm;
        float sensor_width_mm;
        float sensor_height_mm;
        float distance_mm;
        float theta_rad;
    };
}
